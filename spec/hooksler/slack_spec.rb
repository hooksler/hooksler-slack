require 'spec_helper'

describe Hooksler::Slack do
  it 'has a version number' do
    expect(Hooksler::Slack::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
